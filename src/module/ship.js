const ShipFactory = (length) => {
    //takes a number and marks the position as hit
    const hit = (x, y) => {

    }

    //Checks if ship is sunk based on its length and whether all positions are hit
    const isSunk = () => {

    }

    return {length};
}

module.exports = ShipFactory;